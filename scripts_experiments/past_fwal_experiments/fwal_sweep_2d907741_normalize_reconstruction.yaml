program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: 2d907741_normalize_reconstruction

command:
- ${env}
- ${interpreter}
- ${program}
- --test_time_interventions 
- evaluate_test_time_interventions
- --pretrain
- --num_pretrain_steps
- 5000
- --hpc_run
- --num_workers
- 31 # optimal for HPC
- --tags
- 2d907741 # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "Comparing different reconstruction loss normalizations."
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - simple_trig_synth
    - poly_binarised_decimalised_mod10_synth
    - MNIST
    - mice_protein

  normalize_reconstruction:
    distribution: categorical
    values:
    - None
    - num_features
    - num_non_masked_features

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
