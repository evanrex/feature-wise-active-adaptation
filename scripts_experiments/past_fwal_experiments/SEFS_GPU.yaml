program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: a4fb9532_SEFS_GPU

command:
- ${env}
- ${interpreter}
- ${program}
- --model
- SEFS
- --pretrain
- --num_pretrain_steps
- 5000
- --hpc_run
- --num_workers
- 20 # optimal for HPC GPU
- --no_persistent_workers # crashing when we have persistent workers
- --tags
- a4fb9532 
- --notes
- "a4fb9532_SEFS_GPU"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    # - poly_binarised_decimalised_synth
    # - mice_protein
    # - COIL20 
    # # - gisette 
    # - Isolet
    # - madelon
    # - USPS
    - MNIST

    
  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
