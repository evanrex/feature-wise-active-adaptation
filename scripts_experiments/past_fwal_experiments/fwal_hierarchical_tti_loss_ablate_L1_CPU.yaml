program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: 841ef77f_fwal_hierarchical_tti_loss_ablate_L1

command:
- ${env}
- ${interpreter}
- ${program}
- --test_time_interventions 
- evaluate_test_time_interventions
- --hierarchical
- --pretrain
- --num_pretrain_steps
- 5000
- --hpc_run
- --num_workers
- 1 # optimal for HPC
- --tags
- 841ef77f # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "841ef77f_fwal_hierarchical_tti_loss_ablate_L1"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - poly_binarised_decimalised_synth
    - mice_protein
    - COIL20 
    - Isolet
    - madelon
    - USPS
    # - MNIST
    # - PBMC
    - finance
  
  sparsity_regularizer_hyperparam:
    distribution: categorical
    values:
    - 0.0
    - 0.1
    - 0.5
    - 1.0
    - 2.0
    - 10.0
    - 100.0


  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
