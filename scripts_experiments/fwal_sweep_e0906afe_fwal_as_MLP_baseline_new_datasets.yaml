program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: e0906afe_fwal_as_MLP_baseline_new_datasets

command:
- ${env}
- ${interpreter}
- ${program}
- --as_MLP_baseline
- --hpc_run
- --num_workers
- 31 # optimal for HPC
- --tags
- e0906afe # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "e0906afe_fwal_as_MLP_baseline_new_datasets"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - simple_trig_synth
    - poly_binarised_decimalised_mod10_synth
    - MNIST
    - mice_protein

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2

