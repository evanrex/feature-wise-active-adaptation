program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: 73f7aedc_fwal_hierarchical_shared

command:
- ${env}
- ${interpreter}
- ${program}
- --test_time_interventions 
- evaluate_test_time_interventions
- --hierarchical
- --share_mask
- --pretrain
- --num_pretrain_steps
- 5000
- --hpc_run
- --num_workers
- 1 # optimal for HPC
- --tags
- 73f7aedc # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "73f7aedc_fwal_hierarchical shared"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - poly_binarised_decimalised_synth
    - mice_protein
    - COIL20 
    - Isolet
    - madelon
    - USPS
    # - MNIST
    # - PBMC
    - finance

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
