program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: 39efeb67_fwal_only_reconstruct_masked_GPU

command:
- ${env}
- ${interpreter}
- ${program}
- --test_time_interventions 
- evaluate_test_time_interventions
- --pretrain
- --num_pretrain_steps
- 5000
- --only_reconstruct_masked
- --hpc_run
- --num_workers
- 20 # optimal for HPC GPU
- --no_persistent_workers # crashing when we have persistent workers
- --tags
- 39efeb67 # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "39efeb67_fwal_only_reconstruct_masked"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    # - poly_binarised_decimalised_synth
    # - mice_protein
    # - COIL20 
    - gisette 
    # - Isolet
    # - madelon
    # - USPS
    - MNIST

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
