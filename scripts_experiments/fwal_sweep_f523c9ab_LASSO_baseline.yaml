program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: f523c9ab_LASSO_baseline

command:
- ${env}
- ${interpreter}
- ${program}
- --model
- lasso
- --hpc_run
- --num_workers
- 31 # optimal for HPC
- --tags
- f523c9ab # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "f523c9ab_LASSO_baseline"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - simple_trig_synth
    - poly_binarised_decimalised_mod10_synth
    - MNIST
    - mice_protein
  
  lasso_C:
    distribution: categorical
    values:
    - 10
    - 100
    - 1000
  
  lasso_l1_ratio:
    distribution: categorical
    values:
    - 0
    - 0.25
    - 0.5
    - 0.75
    - 1


  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
