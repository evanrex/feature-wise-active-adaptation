program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: b73c5d9f_fwal_as_MLP_baseline

command:
- ${env}
- ${interpreter}
- ${program}
- --model
- fwal
- --as_MLP_baseline
- --valid_percentage
- 0.25
- --sparsity_type
- global
- --gamma
- 1.0
- --sparsity_regularizer
- L1
- --sparsity_regularizer_hyperparam
- 1
- --test_time_interventions 
- evaluate_test_time_interventions
- --hpc_run
- --num_workers
- 31 # optimal for HPC
- --tags
- b73c5d9f # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "b73c5d9f_fwal_as_MLP_baseline"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - simple_trig_synth
    - simple_linear_synth
    - exponential_interaction_synth
    - summed_squares_exponential_synth
    - trigonometric_polynomial_synth

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2

