program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: 9382a057_rf_baseline

command:
- ${env}
- ${interpreter}
- ${program}
- --model
- rf
- --hpc_run
- --num_workers
- 1 # optimal for HPC CPU
- --tags
- 9382a057 # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "9382a057_rf_baseline"
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    # - poly_binarised_decimalised_synth
    # - mice_protein
    # - COIL20 
    # - gisette 
    # - Isolet
    # - madelon
    # - USPS
    # - PBMC
    - finance
  
  rf_max_depth:
    distribution: categorical
    values:
    - 3
    - 5
    - 7

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
