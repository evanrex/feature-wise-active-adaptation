program: /home/er647/projects/feature-wise-active-learning/src/run_experiment.py # Path to the program to run
entity: evangeorgerex
project: fwal # Project name

name: 9624537b_fwal_new_datasets

command:
- ${env}
- ${interpreter}
- ${program}
- --test_time_interventions 
- evaluate_test_time_interventions
- --pretrain
- --num_pretrain_steps
- 5000
- --hpc_run
- --num_workers
- 31 # optimal for HPC
- --tags
- 9624537b # RUN python ./generate_uuid.py in the terminal to generate a new tag for each experiment
- --notes
- "9624537b_fwal_new_datasets."
- ${args}

method: grid
parameters:
  dataset:
    distribution: categorical
    values:
    - poly_binarised_decimalised_synth
    - COIL20 
    - gisette 
    - Isolet
    - madelon
    - USPS

  seed_model_init:  # number of folds
    distribution: categorical
    values:
    - 0
    - 1
    - 2
